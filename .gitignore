# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
.env

# Next.js
.next/
out/
build/
dist/
node_modules/
.DS_Store

# RAG Data
backend/chroma_db/
# Keep embeddings_gemini.json because we need it for serverless!
# We do NOT ignore backend/data/embeddings_gemini.json
# backend/data/*.pdf # Maybe keep PDFs? They are small enough.
backend/models/ # If any local models exist

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
